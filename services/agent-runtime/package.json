{
  "name": "@open-publisher/agent-runtime",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "build": "node ../../scripts/build_pi_runtime.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "bun x --bun vitest run --pool=forks",
    "test:watch": "bun x --bun vitest --pool=forks",
    "test:real": "bun x --bun vitest run --pool=forks --config vitest.real.config.ts"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.83.0",
    "@earendil-works/pi-ai": "0.83.0",
    "@open-publisher/contracts": "file:../../packages/contracts",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.0",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
